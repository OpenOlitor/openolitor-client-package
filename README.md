# openolitor-client-package

This project is used to deliver the OpenOlitor client deliverables (admin and kundenportal) as a single static_buildpack dist.

## Usage

```bash
./package.sh
cf push -f manifest.yml
```
